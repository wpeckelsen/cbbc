# Environment mode — REQUIRED, no default. Must be exactly 'dev' or 'prod'.
# 'dev'  = FTP cache enabled, model limits REQUIRED, brand filter REQUIRED,
#          log level debug.
# 'prod' = cache cleared each run, no model limits (unlimited), brand filter
#          always on, log level info.
ENV=prod

# FTP connection
FTP_HOST=updateftp.duell.fi
FTP_USER=duellus
FTP_PASS=
FTP_PORT=21
FTP_SECURE=false
FTP_REJECT_UNAUTHORIZED=true


# ── Dev-only levers (REQUIRED when ENV=dev, ignored when ENV=prod) ──────────
# Max models promoted to production tables per pipeline run (0 = unlimited).
# PIPELINE_MODEL_LIMIT=5
# Max models pushed to Shopify per push run (0 = unlimited).
# SHOPIFY_PUSH_MODEL_LIMIT=5
# Only process brands listed in brands.md (empty brands.md = allow all).
# BRAND_FILTER_ENABLED=true



# PostgreSQL connection string (Railway)
# when pipeline runs in production in railway, it will use a private db url that is not accessible from outside of railway. 
# That url is set inside Railway. 
# When env=dev, DATABASE_PUBLIC_URL is used. 
# when env=prod, DATABASE_URL is used.

# this is the public db url from the staging env db in railway. it hits the staging db.
DATABASE_PUBLIC_URL=
# this is the public db url from the production env db in railway. it hits the production db.
DATABASE_URL=

# Shopify API config
SHOPIFY_STORE_DOMAIN=
SHOPIFY_CLIENT_ID=
SHOPIFY_SECRET=
SHOPIFY_API_VERSION=2024-10
SHOPIFY_ADMIN_ACCESS_TOKEN=
SHOPIFY_PUBLICATION_ID=
SHOPIFY_LOCATION_ID=

