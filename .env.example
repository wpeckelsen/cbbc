# ---------------------------------------------------------------------------
# CBBC product pipeline — example environment configuration
# Copy to `.env` and fill in. Never commit real secrets.
# ---------------------------------------------------------------------------

# --- Supplier FTP (Duell) ---
FTP_HOST=ftp.example.com
FTP_PORT=21
FTP_USER=
FTP_PASS=
FTP_SECURE=false

# --- Database (direct Postgres, for migrations / introspection) ---
# Any one of these is accepted; DATABASE_URL takes precedence.
DATABASE_URL=postgres://user:pass@host:5432/dbname

# --- Supabase REST API (pipeline reads/writes) ---
SUPABASE_API_BASE_URL=https://your-project.supabase.co
SUPABASE_SECRET_KEY=

# --- Shopify storefront sync ---
# Create a custom app in the Shopify admin (Settings -> Apps and sales channels
# -> Develop apps), grant Admin API scopes: read_products, write_products,
# read_inventory, write_inventory.
SHOPIFY_STORE_DOMAIN=your-store.myshopify.com
SHOPIFY_CLIENT_ID=
SHOPIFY_SECRET=
SHOPIFY_API_VERSION=2024-10
# Access token — obtained via `npm run shopify:auth` (OAuth authorization flow
# using the Client ID + Secret above). You can also paste the token manually if
# you copied it from the Shopify admin.
SHOPIFY_ADMIN_ACCESS_TOKEN=
# Optional: inventory location to write stock to. Leave empty to use the store's
# first active location.
SHOPIFY_LOCATION_ID=
# Optional: weekly cron for the push. Empty = manual only (npm run shopify:push:prod).
# Recommended weekly run is Monday 03:00 (Sunday night -> Monday): 0 3 * * 1
SHOPIFY_PUSH_CRON=
# Force-push all models to Shopify regardless of content hash (bypasses
# skip-if-unchanged optimization). Set to true for first deploy after adding
# hash support, recovery, or manual full-push runs. Default: false.
SHOPIFY_FORCE_PUSH=false

# --- Pipeline worker scheduling ---
CRON_SCHEDULE=0 2 * * *

# --- Misc ---
LOG_LEVEL=info
NODE_ENV=development
DEV_CLEAN_SLATE=false
PIPELINE_DEBUG=0
